import { Meta, Story } from '@storybook/addon-docs';

import { Form } from '../src/core';

import { FieldArray } from './FieldArray';

<Meta
  title="react-final-form-schema/FieldArray"
  component={Form}
/>

export const Template = (props) => {
  return <FieldArray {...props} />;
}

# Field array

<Story name="FieldArray">
  {Template.bind({})}
</Story>
