import { Meta, Story } from '@storybook/addon-docs';

import { Form } from '../src/core';

import { Simple } from './Simple';

<Meta
  title="react-final-form-schema/Simple"
  component={Form}
/>

export const Template = (props) => {
  return <Simple {...props} />;
}

# Simple

<Story name="Simple">
  {Template.bind({})}
</Story>
