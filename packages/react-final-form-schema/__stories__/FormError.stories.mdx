import { Meta, Story } from '@storybook/addon-docs';

import { Form } from '../src/core';

import { FormError } from './FormError';

<Meta
  title="react-final-form-schema/FormError"
  component={Form}
/>

export const Template = (props) => {
  return <FormError {...props} />;
}

# Form error

<Story name="FormError">
  {Template.bind({})}
</Story>
