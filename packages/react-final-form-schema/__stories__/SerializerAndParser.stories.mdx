import { Meta, Story } from '@storybook/addon-docs';

import { Form } from '../src/core';

import { SerializerAndParser } from './SerializerAndParser';

<Meta
  title="react-final-form-schema/SerializerAndParser"
  component={Form}
/>

export const Template = (props) => {
  return <SerializerAndParser {...props} />;
}

# Serializer and parser

<Story name="SerializerAndParser">
  {Template.bind({})}
</Story>
